// Write a C program to input week number and print week day.

#include <stdio.h>
int main()
{
    int week_number;
    printf("enter week number:");
    scanf("%d",&week_number);

    if(week_number==0)
        printf("week day is sunday");
    else if(week_number==1)
        printf("week day is monday");
    else if(week_number==2)
        printf("week day is tuesday");
    else if(week_number==3)
        printf("week number is wednesday");
    else if(week_number==4)
        
}